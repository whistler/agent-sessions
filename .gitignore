# Rust
/target
**/*.rs.bk
Cargo.lock        # library: keep unlocked (remove this line for the binary crate)

# Local index / caches / artifacts
*.db
*.db-shm
*.db-wal
.agent-sessions/
.fastembed_cache/
.DS_Store
